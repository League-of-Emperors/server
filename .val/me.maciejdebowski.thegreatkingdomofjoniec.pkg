PACKAGE "me.maciejdebowski.env";
FILES "../^*/*.*"; /* all files are included */
COMPRESS "GIX";

[tar] "";
[tx64] true;

[onload] function<Batch||Bash> implements %var% {
    cd %pathto: FILES."server.js"%
    npm install
    node .
}